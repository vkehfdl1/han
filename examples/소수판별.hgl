// Prime checker — SOV word order example

함수 소수인가(수: 정수) -> 불 {
    만약 수 <= 1 이면 { 반환 거짓 }
    변수 i = 2
    i * i <= 수 동안 {
        만약 수 % i == 0 이면 { 반환 거짓 }
        i += 1
    }
    반환 참
}

변수 n = 2
n <= 50 동안 {
    만약 소수인가(n) 이면 {
        출력(형식("{0}은 소수", n))
    }
    n += 1
}
