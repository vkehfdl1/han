// Korean logical operator examples (V2)

변수 나이 = 20
변수 학생 = 참

만약 나이 >= 19 그리고 학생 이면 {
    출력("성인 학생입니다")
}

만약 나이 < 13 또는 나이 >= 65 이면 {
    출력("할인 대상입니다")
} 아니면 {
    출력("일반 요금입니다")
}

// Legacy && and || also work
만약 나이 >= 19 && 학생 이면 {
    출력("(기존 문법도 OK)")
}
